# Frontend example env variables
# When running the frontend dev server, set REACT_APP_API_BASE to point to your backend.
# Default backend dev port suggestion is 3001 to avoid collisions with CRA dev server (3000).
REACT_APP_API_BASE=http://localhost:3001
